import React, { useRef, useState } from 'react'

type Props = {}

export default function TheUseLayoutEffect({}: Props) {

  //Sera executado antes que o DOM estiver montado diferente do LayoutEffect que sera montado depois

  // const [show, setShow] = useState(false)

  // const popup = useRef()
  // const button = useRef()

  // useEffect(() => {
  //   if(!popup.current || !button.current) return

  //   const { button } button.current
  
  //   return () => {
      
  //   }
  // }, [])
  

  return (
    <div>TheUseLayoutEffect</div>
  )
} 